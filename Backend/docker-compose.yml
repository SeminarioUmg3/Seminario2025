services:
  backend:
    command: bash -c "npm install && npx prisma generate && npm run dev"
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    expose:
      - 8000
    volumes:
      - .:/app
    env_file:
      - .env
